// Stubbed carousel to revert state before shadcn carousel installer.
// This file intentionally exports a harmless component so the codebase
// behaves as if the carousel was never added. If you want the original
// carousel back later, restore from git or re-run the installer.

"use client";
import React from "react";

export default function Carousel() {
  return null;
}
